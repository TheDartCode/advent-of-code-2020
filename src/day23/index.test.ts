import day23 from "./index";

const TEST_DATA = `32415`;

describe("day23", () => {
  describe("first part", () => {
    it.skip("satisfies test data", () => {
      expect(day23("a", TEST_DATA)).toBe("2");
    });
  });
  describe("second part", () => {
    it.skip("satisfies test data", () => {
      expect(day23("b", TEST_DATA)).toBe("1");
    });
  });
});
